<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/cameraFragment">

    <fragment
        android:id="@+id/cameraFragment"
        android:name="com.example.vkrandroid.CameraFragment"
        android:label="CameraFragment"
        tools:layout="@layout/fragment_camera">
        <action
            android:id="@+id/action_camera_to_gallery"
            app:destination="@id/galleryFragment" />
    </fragment>

    <fragment
        android:id="@+id/galleryFragment"
        android:name="com.example.vkrandroid.GalleryFragment"
        android:label="GalleryFragment"
        tools:layout="@layout/fragment_gallery">

        <action
            android:id="@+id/action_gallery_to_viewer"
            app:destination="@id/viewerFragment" />

    </fragment>

    <fragment
        android:id="@+id/viewerFragment"
        android:name="com.example.vkrandroid.ViewerFragment"
        android:label="ViewerFragment"
        tools:layout="@layout/fragment_viewer">
        <argument
            android:name="filePath"
            app:argType="string" />
        <argument
            android:name="isVideo"
            app:argType="boolean" />
    </fragment>
</navigation>